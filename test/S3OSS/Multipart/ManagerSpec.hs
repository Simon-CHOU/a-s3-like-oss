module S3OSS.Multipart.ManagerSpec (spec) where

import Test.Hspec

-- Integration tests for multipart manager will be added.
spec :: Spec
spec = do
  describe "Multipart Manager" $ do
    it "placeholder test" $ do
      1 + 1 `shouldBe` (2 :: Int)
